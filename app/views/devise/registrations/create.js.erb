//============================================================
// DISPLAYS THE MODAL WITH THE <script> TAG
//============================================================

var domainId = "<%= @user.domains.last.id %>";

//reload top nav now that they are logged in
$(".nav").html("<%= escape_javascript(render(:partial => "layouts/navigation"))%>");

// append script tag onto div
displayCode(domainId);

// show the modal
$('.ui.modal').modal('show');