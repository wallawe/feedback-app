//update the values for the user's form they will be putting on their site, so it saves comments properly
var domainId = "<%= @user.domains.last.id %>";

$('#feedback_form_0243').attr('action', 'http://localhost:7000/domains/' + domainId + '/comments' );
$('#domain_id').attr('value', domainId );

//reload top nav now that they are logged in
$(".nav").html("<%= escape_javascript(render(:partial => "layouts/navigation_links"))%>");

displayCode();

$('.ui.modal').modal('show');